# CPU-only PyTorch keeps the install small enough for Streamlit Cloud's limits.
# The extra index is where the "+cpu" builds live.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.2.2+cpu
torchvision==0.17.2+cpu
streamlit>=1.33
pillow>=10.0
numpy>=1.24